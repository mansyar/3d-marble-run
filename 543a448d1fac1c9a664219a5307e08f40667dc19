Task: Add the non-snapped Drop point placement controller. Summary: Added a pure DropPointState editor backed by the generic command stack, normalizing free X/Z placement, replacing the single point, deleting, and undoing/redoing without any TrackGraph dependency or mutation. Files: src/build/dropPointPlacement.ts and tests/dropPointPlacement.test.ts. Rationale: keep Drop point edits as a separate persisted setting while reusing the project’s command-stack semantics. Verification: targeted tests passed (4), isolated editor coverage was 94.44% statements / 100% lines, Biome and TypeScript passed.
