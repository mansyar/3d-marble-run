Task: Add the non-snapped Drop point placement controller. Summary: Added a dedicated Drop point tray selection, free table-plane pointer/touch placement controller, single-point editing hooks, keyboard deletion/undo/redo, and main wiring that keeps the physical-piece placement and snapping path separate. The controller owns only DropPointState and its command stack; it never receives or mutates TrackGraph. Files: src/build/dropPointController.ts, src/ui/tray.ts, src/main.ts. Rationale: replace physical Start-gate placement with a separate free X/Z tool while preserving existing piece editing behavior. Verification: targeted placement and piece tests passed (22); Biome and TypeScript passed.
