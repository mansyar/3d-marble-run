Route guidance: health.ts BFS refactor (shared bfsParents with parent pointers + parentsToPath); new exports unreachableConnectorPieces (connector-bearing pieces not BFS-reachable from the Drop-point landing piece; portless pieces exempt) and routePathsToGoals (GoalRoute {goalId, pieceIds} per reachable goal cup in BFS discovery order). Rendering: new src/render/guidance.ts layer — violet (0x8338ec, Drop-point accent) emissive pulse on unreachable piece materials (emissive originals restored on untrack; prefers-reduced-motion -> static 0.22 intensity, no animation), soft additive TubeGeometry glow (radius 0.025, hover 0.1) along each route's piece origins, rebuilt only on refresh (graph/drop-point edits via refreshDropPointHealth), per-frame tick only advances a sine phase when pieces are tracked. app.ts wiring: guidance layer created after scene init (originOf from graph placements, pieceGroupOf from spawned groups), refresh hooked into refreshDropPointHealth, tick into the render-loop callback. RED tests 5ea9560; helpers GREEN 10/10 dropHealth; full gate: Biome clean (107 files), 238/238 tests, build OK, payload 3,491.77 kB min / 1,245.14 kB gzip within budget 3,500/1,250 (gzip headroom now 4.9 kB - tight).
