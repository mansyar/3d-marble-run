Task: Route optional Coolify deployment through eligible outputs.\nRationale: Make validated release tags the automatic production path while retaining an explicit default-branch manual override.\nFiles: .github/workflows/publish-container.yml.\nChanges: Coolify now requires a successful GHCR publication, accepts non-manual v* tag contexts from release orchestration or default-branch workflow dispatch, rejects manual tag/non-default refs, and remains opt-in through the existing variable and secrets.\nVerification: git diff --check passed; existing webhook, bearer authentication, retry, and timeout behavior were preserved. actionlint is unavailable, so the expression was manually reviewed.

Phase 2 checkpoint verification: release-only publication policy and master CI. Automated verification passed: 92 Vitest tests across 12 files, Biome, TypeScript, production build, matching pnpm check:release for v0.1.0, expected rejection for mismatched v0.1.1, and git diff --check. Build measured 3,422.17 kB raw JavaScript / 1,240.30 kB gzip; existing large-chunk warning remained non-failing. Workflow review confirmed no master push triggers on Pages/GHCR, default-branch-only manual guards, tag-only release orchestration, quality gating before publication, preserved Pages base path, GHCR exact/latest/SHA metadata and OCI labels, least-privilege permissions, reusable calls, and release/manual-only Coolify eligibility after successful publication. actionlint was unavailable; syntax and expressions were manually reviewed. User confirmed desktop and mobile/narrow-viewport preview smoke tests: editor loading, piece editing, marble drop, About dialog, touch controls, and canvas interaction. Remote CI/release execution remains post-push verification.
