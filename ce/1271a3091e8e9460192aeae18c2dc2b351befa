Task: Add the master-push CI-only workflow.\nRationale: Keep master feedback through deterministic quality checks without deploying Pages or publishing GHCR artifacts.\nFiles: .github/workflows/ci.yml.\nVerification: git diff --check passed; actionlint was unavailable, so workflow syntax and permissions were manually reviewed. Full local quality gates are covered by the following validation task.

Phase 1 checkpoint: Master CI-only validation. Automated verification: 92 Vitest tests passed, Biome passed, TypeScript passed, production build passed, and git diff --check passed. Build measured 3,422.17 kB raw JavaScript / 1,240.30 kB gzip with the existing large-chunk warning. actionlint was unavailable; workflow trigger, permissions, and steps were manually reviewed. No application logic changed, so coverage was not applicable. User confirmed desktop and mobile/narrow-viewport smoke tests: editor loading, piece editing, marble drop, About dialog, touch-sized controls, and canvas interaction. Remote CI execution remains post-push verification.
