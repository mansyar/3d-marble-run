Task: Migrate version-1 Start-gate saves (Red phase). Summary: Added tests for connected and unconnected gate migration to a fixed-height Drop point, removal of the gate-only edge while preserving physical connections and nextId, and rejection of invalid legacy positions or references. Files: tests/serialization.test.ts. Rationale: lock down v1 compatibility and prevent corrupt legacy graphs from becoming active v2 documents. Verification: targeted Red run failed only on the two migration expectations because v1 documents still returned a null Drop point and retained the gate.
