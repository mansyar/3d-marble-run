Phase 5 - Docs task: README polish notes
product.md and product-guidelines.md reviewed - no drift (quality toggle already allowed in non-goals; lighting/gloss wording already matches retune), so untouched. README updated: intro names subtle bevel + glossy candy plastic on warm wood table; route guidance paragraph notes stronger pulse/glow on narrow screens; new paragraph documents the Quality toggle (Auto caps resolution/shadows for phone smoothness, High lifts caps, persists across visits). Tone kid-safe, sentence-case. No payload impact (docs only).

Phase 5 Final Verification Report - checkpoint confirmed by user
Automated final gate: biome check clean; vitest 265/265 green; src/core coverage 98.83% stmts / 97.36% branch / 100% funcs / 100% lines (quality.ts 100% across all metrics); tsc --noEmit + vite build green; check:size TOTAL 3,499.97 kB min / 1,247.75 kB gzip vs budget 3,500/1,250 - payload within budget. Track delta vs branch-point baseline (~3,498.5 min / ~1,247.3 gzip): ~+1.5 min / ~+0.5 gzip, within spec cap <=2.0/<=1.2 kB.
Manual protocol (desktop 1280x720 + touch 393x659): starter ready route with violet drop-to-cup glow; tray single-row scroll with momentum/snap/fades on phone; Splitter forks off-center marbles, Bumper free-stands and bounces; guidance pulse visible on phone and stops on connect (bumpers never pulse); Quality toggle Auto/High hot-updates shadows + DPR and persists; anti-jam nudge then recycle keeps stream fluid, no false goal credits; save/autosave round-trip on reload; PWA offline replay via preview; SFX toggle persists; 20-marble stream fluid on both viewports.
User confirmation: explicit yes - polish meets the proudly-say-play-my-game bar.
