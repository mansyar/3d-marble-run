Glow-path fix (user checkpoint feedback: glow swung outside the rails on bends): route polylines now bend at the actual channel points — for each hop the shared port's world position (new GuidanceDeps.connectionPointOf via getWorldPort) plus each piece's centre — and CatmullRomCurve3 tension 0 makes runs straight between those points, so the tube can never leave the corridor (curve-piece chords stay within the 0.3 half-width). Dedupes coincident points; tube segments 4/run. Gate: Biome clean (107 files), 238/238 tests, build OK.
