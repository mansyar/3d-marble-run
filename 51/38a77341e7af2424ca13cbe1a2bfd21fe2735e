Service worker (task 2.4, pwa_budget_20260827): vite-plugin-pwa 1.3.0 (dev dep) with registerType autoUpdate (skipWaiting + clients.claim — new deploys activate silently on next load), injectRegister auto, manifest:false (static public/manifest.webmanifest from task 2.3 is the single source), workbox globPatterns covering js/css/html/png/webmanifest, maximumFileSizeToCacheInBytes raised to 4 MiB because the default 2 MiB limit rejected the 3.44 MB async app chunk carrying Rapier's WASM (plugin correctly failed the build rather than silently breaking offline play). Dev stays SW-free (devOptions disabled). Verified: build exit 0, dist gains sw.js + locally-bundled workbox runtime + registerSW.js injected in index.html, precache includes entry HTML, CSS, both chunks, manifest and all four icon PNGs, zero external CDN references in sw.js; gates: 213/213 tests, tsc clean, biome clean, check:size 3,472.10 kB min / 1,239.56 kB gzip within budget.
