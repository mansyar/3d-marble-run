Bumper GREEN phase: registry entry (portless PieceTypeDef, PieceTypeId union extended); isPieceTypeId allow-list now accepts splitter AND bumper (fixes latent Phase 1 serializer gap - splitter could not round-trip saves before this); PIECE_COLORS.bumper = 0xf4978e light coral (red-family tint, <=6 hue budget kept); buildBumper = lathe dome profile (R 0.22, H 0.28, 24 seg) with matching trimesh collider; ColliderSpec gained optional restitution, applied in toColliderDesc via setRestitution - BUMPER_RESTITUTION 0.85; tray label 'Bumper'. Zero placement-code changes: classifySnap returns free for zero-candidate portless drags, so free table placement/ghost/move/delete/undo-redo fell out of the existing controller. Physics test pins head-on block + lively rebound. Gate: Biome clean, 234/234 tests, build OK, payload 3475.77 kB min / 1240.70 kB gzip within budget.
