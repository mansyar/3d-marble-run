Task: First-launch pre-built starter contraption; subsequent launches load last autosave\n\nSummary: Added a five-piece starter graph with a descending ramp, straight, curve, funnel, and goal cup connected through symmetric graph ports. Startup now loads the persisted __autosave__ graph before rendering and persists the starter graph on first launch when no autosave exists, with a starter fallback if storage initialization fails.\n\nFiles: src/track/starter.ts, src/track/startup.ts, src/main.ts, and tests/starter.test.ts.\n\nRationale: Give first-time players an immediately readable, playable contraption while ensuring reloads preserve their latest edited graph instead of reseeding over it. The startup helper keeps the IndexedDB decision isolated and testable, and the default drop point lands above the starter ramp.\n\nVerification: CI=true pnpm vitest run --coverage passed 10 files / 77 tests with 81.17% statements, 78.10% branches, 91.00% functions, and 82.43% lines overall; starter.ts and startup.ts were 100% covered. CI=true pnpm biome check . passed. pnpm build passed TypeScript/Vite with the existing large-bundle warning (3,416.84 kB JS / 1,238.59 kB gzip). User confirmed first-launch, autosave reload, fallback, and mobile behavior.
