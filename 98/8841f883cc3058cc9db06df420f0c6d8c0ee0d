Task: Implement debounced auto-save + named-slot UI\n\nSummary: Added versioned track serialization, IndexedDB storage through idb, debounced autosave with pagehide flush, named save/load/delete/list operations, responsive touch-sized save controls, graph replacement on load, command-history clearing, and placement edit callbacks that schedule autosaves. The marble spawn point is now y=4 per user request.\n\nFiles: package.json, pnpm-lock.yaml, src/track/serialization.ts, src/track/storage.ts, src/ui/save-slots.ts, src/build/placement.ts, src/core/commandStack.ts, src/main.ts, src/style.css, tests/commands.test.ts, and tests/storage.test.ts.\n\nRationale: Keep saved graphs JSON-safe and independent, hide the internal autosave slot from named-slot UI, debounce writes so edits do not cause excessive IndexedDB traffic, clear stale undo history after loading a graph, and preserve touch accessibility.\n\nVerification: CI=true pnpm vitest run --coverage passed 74 tests with 80.50% statements, 77.84% branches, 90.81% functions, and 81.77% lines overall; storage statements were 89.47% and serialization statements 84.61%. CI=true pnpm biome check . passed. pnpm build passed TypeScript/Vite with the existing large-bundle warning. User confirmed named save/load/delete and autosave behavior manually.
