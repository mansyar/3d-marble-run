Added non-blocking first-run coach marks for choosing, placing, and dropping. Progress is remembered locally, can be dismissed, advances for physical pieces and the Drop point, and uses pointer-events plus reduced-motion CSS so the canvas/tray remain usable. Browser checks passed at 390x844 and 1280x720 with no application errors.

Phase 2 checkpoint verified: 25 Vitest files and 146 tests passed; Biome checked 80 files; strict TypeScript passed; production build measured 3,436.14 kB JavaScript and 1,244.46 kB gzip within budget. Browser checks passed at 360x800, 390x844, and 1280x720: first-run coach progression, Drop point progression, persistent dismissal, responsive HUD, touch-sized controls, keyboard Enter/Escape placement parity, no horizontal overflow, and reduced-motion animation disabled. Console had 0 application errors and 1 existing Three.js deprecation warning. User confirmed the Phase 2 checkpoint on 2026-08-26.
