Review fixes (conductor-review): (1) MEDIUM - scripts/check-bundle-size.mjs parseBudgetValue validates CLI numeric args (Number.isFinite, empty string rejected) and main() exits 1 with a clear message instead of NaN silently passing the gate; probe --min-kb abc now exits 1. (2) LOW - src/core/boot.ts begin() now honors its once contract with a phase guard (busy or phase!==loading returns); 2 new tests. (3+4) LOW - src/main.ts retry button reloads the document: the ES module registry caches evaluation failures so only a fresh document recovers from both chunk-fetch and evaluation-time errors; SW-era reloads serve from precache; reload semantics also eliminate the lingering failure panel (finding 3) with no extra code. (5) LOW - public/manifest.webmanifest is now generated by scripts/generate-icons.mjs (MANIFEST_META + buildManifest, relative icon srcs by default) so icon sizes and metadata share one source; round-trip verified byte-identical to the hand-authored file. Tests: +9 (222/222), tsc clean, biome clean (105 files), build OK, gate 3,472.13 kB min / 1,239.58 kB gzip within budget.
