Task: Collapse named saves behind a Saved tracks control\n\nSummary: Wrapped named-save controls in an inline responsive drawer with a compact trigger, heading, close control, Escape/outside-touch closing, and focus return. Existing save/load/delete callbacks and live status output remain unchanged.\n\nFiles: src/ui/save-slots.ts, src/style.css\nWhy: Named saves are secondary to play and previously occupied a permanent mobile panel that competed with status guidance.
