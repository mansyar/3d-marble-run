Checkpoint-fix round 2 (user feedback): (1) 10249fe - CURVE_SEGMENTS 8->16 halves the rail joint angle on curve/splitter arcs, removing the chunky segmented-fin look; physics unchanged (same ports/radius, finer cuboid discretization). (2) babec33 - portless tools (bumper) now seat on whatever track surface is under the pointer (raycast against rendered piece groups, first hit wins, table plane as fallback), so bumpers can live on funnel bells, ramps and rails instead of sinking to the floor; connector pieces keep the table-plane behavior and let classifySnap decide their pose. Gate: Biome clean, 234/234 tests, build OK, payload 3,475.77 kB min / 1,240.70 kB gzip within budget.
