Task: Replace the fixed spawn position with gate-driven spawning (adapter)\nSummary: Added gate-aware manual and stream adapters that attach the resolved world anchor to spawn events and stop continuous scheduling when the gate is absent.\nFiles: src/sim/gateSpawner.ts\nWhy: Give the main loop a single guard that prevents invisible spawned state while preserving the existing pure scheduler.
