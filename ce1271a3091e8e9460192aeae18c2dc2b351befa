Task: Add the master-push CI-only workflow.\nRationale: Keep master feedback through deterministic quality checks without deploying Pages or publishing GHCR artifacts.\nFiles: .github/workflows/ci.yml.\nVerification: git diff --check passed; actionlint was unavailable, so workflow syntax and permissions were manually reviewed. Full local quality gates are covered by the following validation task.
