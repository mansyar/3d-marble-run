Task: Implement tray HUD + ghost placement flow (desktop pointer events + touch gestures)\n\nSummary: Added the bottom tray for all five piece types, translucent pointer/touch placement ghosts, snap/blocked/free feedback, rotation controls, cancellation, and PlaceCommand integration. Extended funnel compatibility so run pieces can use the mouth or spout where available, and persisted snap connections in the graph. Corrected curve port geometry and ramp transform handling for reliable placement.\n\nFiles: src/build/placement.ts, src/ui/tray.ts, src/main.ts, src/style.css, src/render/scene.ts, src/pieces/builders.ts, src/pieces/registry.ts, src/track/snapping.ts, src/track/commands.ts, tests/commands.test.ts, tests/pieces.test.ts, tests/snapping.test.ts, conductor/tracks/mvp_20260824/plan.md.\n\nWhy: Give build mode a usable desktop/mobile placement flow while keeping port geometry, graph occupancy, and undoable placement state consistent. Verification: 51 Vitest tests passed; coverage 97.38% statements / 99.39% lines overall; Biome and pnpm build passed; user confirmed the visual placement and piece behavior.
