Task: Define the Drop point model and fixed-height rules — Red phase. Added tests for nullable Drop point state, fixed Y=4 normalization, X/Z validation, and immutable one-point replacement. Files: tests/dropPoint.test.ts. Rationale: establish the separate persisted drop-point contract before implementing it. Verification: targeted test run failed as expected because src/track/dropPoint.ts was not yet implemented.
