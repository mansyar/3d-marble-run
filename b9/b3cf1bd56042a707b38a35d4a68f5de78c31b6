Glow path fixes from checkpoint feedback: (1) 51423c4 route glow swung outside the rails because the CatmullRom spline (tension 0.5) only bent at piece centres — now the polyline bends at each hop's shared-port world position (new GuidanceDeps.connectionPointOf via getWorldPort over the graph connections) with tension 0 straight runs between channel points, so the tube stays inside the corridor; (2) b9b3cf1 the glow dipped below/inside ramps — piece path points are now the centroid of the piece's ports' world positions (channelPointOf replaces originOf; ramp centroid = chord middle at the correct rise height; straight/curve unchanged; funnel/cup collinear with the axis; portless pieces fall back to the placement origin). Gate after both: Biome clean (107 files), 238/238 tests, build OK.
